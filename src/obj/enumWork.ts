export enum Work {
    GROW = "/bin/grow.js",// = ns.grow,
    HACK = "/bin/hack.js",// = (ns:NS, serverName:string)  => ns.hack(serverName),
    WEAK = "/bin/weak.js"// = (ns:NS, serverName:string)  => ns.weaken(serverName)
}